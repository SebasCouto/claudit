{
  "description": "claudit — al INICIAR la sesion genera el reporte HTML en .claudit/<slot>_<ts>__<id>/ (panorama del setup fijo, una sola vez por sesion)",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/claudit.py\" --html --force",
            "timeout": 20,
            "async": true
          }
        ]
      }
    ]
  }
}
